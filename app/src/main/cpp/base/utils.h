#pragma once

#include <stdlib.h>
#include <stdio.h>
#include <string>
#include <vector>
#include <string.h>

using namespace std;


long long GetCurrentTimeMillis();
#define ZERO_MEM(a) memset(a, 0, sizeof(a))